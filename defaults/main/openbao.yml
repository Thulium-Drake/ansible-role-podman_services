---
# The listening URI for openbao
# Either specify an IP address:port (e.g. 127.0.0.1:8200) or a port for listening on all interfaces
# NOTE: OpenBao is not encrypted by default!
openbao_listen_uri: '8200'
